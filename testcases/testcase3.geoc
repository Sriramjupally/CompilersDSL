struct Mystruct{
    int a = 2, i, j[12];
    bool b;
    double c;
    long d;
    point p, q;
    triangle t;
};

int main(){
    point p3;
    point p1, p2 = p3; 
    int x,y,z;
    struct Mystruct car;
    car.p = p3;
    car.b = ( ((y < z) && (y < x)) || (((x < z) && (x < y)))  ); 
    return 0;
}